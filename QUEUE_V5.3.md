# v5.3 队列版：实现与验收

## 模块与约定

继续使用 Python，未迁移 Electron。现有 `REPORT_CONFIGS`、字段映射、普通数据自由查询及清理规则保留，不修改历史参数表、生成器或 Win7 独立登录捕获小工具。

| 文件 | 职责 |
|---|---|
| `TMIS数据批量抓取工具V5.0_副本.py` | 报表配置、无界面的 `ReportEngine`、入口、可选后处理和旧捕获入口 |
| `tmis_queue.py` | 文件校验、参数快照、SQLite 事务、实例锁、任务/尝试记录、CSV |
| `tmis_service.py` | 独立浏览器会话、串行消费者、登录/暂停/追加/重试/退出 |
| `tmis_ui.py` | Tk 多选及任务列表，所有控件在主线程更新 |
| `tmis_runtime.py` | 日期和路径校验、浏览器兼容辅助、错误脱敏、防覆盖发布 |

后台线程拥有唯一 asyncio 循环、浏览器及单消费者。Excel 在线程池解析，不阻塞网页查询；导入锁保证多表次序。任务事务内认领并创建尝试记录；队列为空时消费者保持待命。

新批次、非空参数变为空及失败恢复时重载查询页面，避免继承上次选择。明确的会话失效暂停整个队列。不会产生额外保活请求或绕过服务端会话有效期。

重试只接纳失败/超时/中断项并排到队尾。普通开始恢复全部中断项；选择重试不顺带恢复未选中的中断项。成功项不自动重跑，即便文件被用户手动删除。

下载防覆盖；后处理失败保留原下载，允许仅重试后处理。崩溃发生在“文件已落盘但状态未入库”的极小窗口时，恢复可能提示同名冲突，需要人工确认文件。不承诺文件系统与数据库之间绝对一次提交。任务库单实例、WAL/FULL 同步；备份前应退出。

## 自动化验证

```bash
.venv/bin/python -B -m unittest discover -s tests -p 'test_*.py' -v
```

实际浏览器与本地模拟页面：

```bash
TMIS_BROWSER_TESTS=1 \
TMIS_TEST_BROWSER_PATH='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome' \
.venv/bin/python -B -m unittest discover -s tests -p 'test_report_browser.py' -v
```

Tk 控件回归，需可用的 Tk 和显示环境；Linux CI 使用 Xvfb：

```bash
TMIS_GUI_TESTS=1 python3 -B -m unittest discover -s tests -p 'test_report_ui.py' -v
```

浏览器 fixture 仅绑定 `127.0.0.1`，模拟收入/支出/库存表单及报表 iframe，返回本地测试文件并校验落盘字节；不把它冒充真实 TMIS 报表。状态测试使用临时任务库，不污染用户数据。GitHub 构建配置也已加入这些测试，但本轮未推送或触发构建。

## 目标机验收清单

2026-09-17 本地结果：48 项逻辑/参数/存储回归、8 项真实浏览器模拟页回归、6 项 Tk 控件回归均通过；v5.3 源码自检通过。此前生成的收入 280 条、支出 280 条及省级库存 20 条共 580 条参数均能完整导入；报表配置与字段映射对照旧版保持不变。

逻辑/浏览器测试使用现有 Python 3.9 + Playwright 1.49 和独立系统 Chrome；Tk 控件及完整源码自检使用隔离 Python 3.12 + Tk 9.0.4。本机系统自带 Tk 8.5 的空窗口测试出现卡顿，未修改原运行环境。原生界面已启动、控件测试通过，但当前桌面工具无法识别该独立 Python 进程，未完成截图外观复核。以上不代表 Tk 8.6 / Linux 包已验收。

1. 新版包自检通过，界面显示 v5.3。
2. 不选表、不选目录，先粘贴 URL，进入固定报表但不查询。
3. 同时加入收入、支出、库存小样本；确认地市和省级参数及独立输出目录。
4. 运行中加入新表，不打断当前条，自动从队尾接续。
5. 暂停等待当前条结束，浏览器不关；继续只运行剩余项。
6. 可控超时/无效选项任务进入失败列表；选中重试，成功项次数不变。
7. 浏览器关闭或登录过期后，剩余任务保留，新 URL 登录后可继续。
8. 批次结束浏览器仍开，新批次可用；确认退出才关闭服务和浏览器。
9. 中途退出并重启，登录后恢复中断/待执行项，对照 CSV 与尝试历史。
10. 重复导入或预放同名文件时有提示，不覆盖原文件。

本轮没有访问真实 TMIS、推送 GitHub 或执行 Actions；新版本仍需重新构建及麒麟 V10 SP1 x64 实机验收。
