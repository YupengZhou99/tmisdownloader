# TMIS 数据自由查询下载工具 v5.3 队列版：麒麟 V10 SP1 x64

新 Electron 桌面版请阅读 **[V6 使用说明](DESKTOP_V6.md)**。本页仅对应保留的 Tk 兼容入口，旧工作流改为手动触发。

用于麒麟图形桌面的普通“数据自由查询”，支持收入、支出、退库、库存，不是“总库数据自由查询”。保留 Python + Tk + Playwright；主程序文件名仍为 `TMIS数据批量抓取工具V5.0_副本.py`。

本页描述保留的 Tk 兼容入口；当前新发布请使用 V6 工作流。旧 v5.2 包不会自动获得下述队列功能，如需 Tk 版可手动触发旧工作流构建。实现与验收清单见 [队列版说明](QUEUE_V5.3.md)。

## 解压和启动

新版本完成打包后，从 GitHub Actions 下载 `TMIS-Kylin-V10-x64` 产物，解开外层 ZIP 后，将其中的 `TMIS-Kylin-V10-x64.tar.gz` 复制到麒麟电脑。建议在自己的主目录解压并运行：

```bash
tar -xzf TMIS-Kylin-V10-x64.tar.gz
cd TMIS-Data-Free-Query
./启动.sh
```

整个 `TMIS-Data-Free-Query` 文件夹需要保留，包括 `_internal` 和 `browser`。无需另装 Python、pip 或下载浏览器。应在 Linux 文件系统上解压，保留可执行权限和符号链接。

启动过程：

1. 点击“粘贴链接并登录”，粘贴本次有效的完整 HTTP(S) URL。程序立即打开自己的浏览器，看到“固定报表”后显示已登录。**不必先选参数表或目录。**
2. 点击“选择（多选）”，可同时选收入、支出、库存等多份参数表；选择保存根目录。
3. 保持“参数表命名”，默认关闭“启用数据后处理”，点击“加入队列”。每份表创建独立下载子目录。
4. 核对任务列表后，点击“开始 / 继续”。也可先加入任务再登录；点击开始时尚未登录，会保留任务等待登录。
5. 运行中仍能多选新表并加入队尾，不打断当前条。运行时追加会接着执行；空闲或暂停时追加只入队，需点击开始。

Linux 界面没有登录 URL 捕获按钮，也不会启动捕获线程。无需另行手动启动浏览器。URL 失效时，重新获取有效 URL，再通过同一入口粘贴。

浏览器路径留空时，Linux 包优先使用附带的 Chromium；没有随包浏览器时才查找系统 Chrome/Chromium。需要使用特定系统浏览器时，在“浏览器路径”填写可执行文件的完整路径。手动路径无效会明确报错，不会静默切换。

## 已有江苏参数表

本次代码更新不修改已有 Excel。继续使用此前确认的收入 280 条、支出 280 条及省级库存本级 20 条表。**以下随旧包附带的 280 条库存模板是早期版本，其中省级辖属仍为“0 -- 全辖”。需要后来确认的省级“1 -- 本级”时，请使用重做的省级库存参数表，不要误用旧模板的省级行。**

时间为 2025 年 1 月至 2026 年 8 月，共 20 个月。每个月按以下 14 个国库逐一下载，每条输出一个文件：

| 国库范围 | 数量 | 展示范围 | 报表数量 |
|---|---:|---|---:|
| 1002000000、1003000000、1004000000、1005000000、1006000000、1007000000、1008000000、1009000000、1010000000、1011000000、1012000000、1013000000、1015000000 | 13 | `0 -- 全部` | 260 |
| 1000000000（省级） | 1 | `1 -- 下级` | 20 |

“按月下载”指每次查询一个完整月份的日数据，**报表类型为 `1 -- 日`**。例如 2025 年 2 月起止日期为 `20250201`、`20250228`，2026 年 8 月为 `20260801`、`20260831`。参数表保存为真实 Excel 日期，显示为八位日期，程序读取后转换为页面需要的 `YYYYMMDD`。

固定条件：

- 报表库：`1 -- 报表库`；辖属：`0 -- 全辖`；国库属性：`0 -- 全部`。
- 预算级次：`0`；会计科目：`17202,271,27201,27202,27203,273`。
- 会计账户名称留空，沿用新页面未指定账户的状态。
- 计划单列市/县/乡：`0 -- 不含`；金额单位：`0 -- 元`。
- 勾选：分序时、分地区、分预算级次、分会计科目、分会计账户。
- 取消：分国库属性、是否展示同比。

参数表中每条任务都有完整查询条件。文件名例如 `江苏库存_202501_1002000000_全部.xlsx` 和 `江苏库存_202608_1000000000_下级.xlsx`。后缀以系统实际导出的格式为准。

## 队列、失败重试与恢复

每条任务显示来源、工作表、行号、国库、日期、状态、次数、阶段和错误。批次结束如有失败/超时，自动显示失败列表；选中一条或多条点击“重试选中”，或使用“全部失败重试”。重试项排到队尾，成功项不重跑；双击可查看固定参数、输出路径和历次尝试，重试成功后从失败列表移除，原记录保留。

点击“暂停（当前条完成后）”会等待当前条结束，再暂停取任务，**浏览器保持开启**。整轮任务结束后服务进入空闲，可继续加表或重试。只有关闭主窗口/点击退出并确认，才关闭本程序浏览器和后台服务；不隐藏到托盘，不在关窗后后台执行。

任务与每次尝试自动保存到本机 SQLite。下次启动会恢复列表但不自动下载，重新登录后点击开始恢复待执行/中断项；失败/超时项通过重试恢复。浏览器关闭或检测到登录失效时，整个队列等待重新登录，不把所有剩余任务逐条跑成失败。检测依赖 401、登录页或明确过期提示，无法识别的服务端异常仍可能先表现为超时。

导入时固定参数、命名、后处理选项和输出目录；后来修改 Excel 或界面选项不影响已入队任务。恢复不要求原 Excel 仍在原位置，但下载子目录须存在且可写。表内日期优先；缺少有效日期、重复表头、错误复选框或同批次重名时，整份表不入队，提示工作表和行号，其他合格文件继续导入。不要把说明页改成含报表类型的名字。

相同参数重复导入会询问是否另建批次。下载不会覆盖已有报表；遇到同名文件先核对并移走/改名，再重试。“清空待选文件”只清理未提交选择，不影响已经入队的任务。下载先保存临时文件，再防覆盖发布；不支持硬链接的文件系统使用独占创建，意外断电可能留下同名未完整文件，恢复时也不会覆盖。下载成功但后处理失败时，重试仅执行后处理，原下载文件保留。

每个“表名_时间_批次ID”子目录同时包含 `下载结果.csv`（最新状态）及 `重试记录.csv`（历次尝试）。时间使用带时区的 UTC。CSV 是便于查看的副本，恢复以 SQLite 为准；CSV 写入失败会提示，不丢弃数据库状态。

Linux 任务库位于 `~/.local/state/tmis-dler/queue.sqlite3`（遵循 `XDG_STATE_HOME`）；macOS 源码运行位于 `~/Library/Application Support/TMISDLer/queue.sqlite3`。可用 `TMIS_STATE_DIR` 指定隔离目录。同一任务库只允许一个实例。备份前正常退出，再备份整个任务库目录和报表目录，不要运行中删除数据库、WAL 或锁文件。

任务库包含参数、来源路径、输出路径和历史，不保存登录 URL、token 或 Cookie，错误日志对 URL 及常见凭据字段脱敏。重新启动须提供新的有效链接；“浏览器保持开启”不保证 TMIS 永不过期，也不产生保活请求。请勿将任务库或真实报表上传公共仓库。

## 兼容性和验证范围

构建目标为 Linux x86_64、glibc 2.31 或更新版本，并要求图形桌面/X11。GitHub runner 内使用 Debian 11 容器构建，包含 Python 3.11、Tk、固定版本依赖和 Chromium。运行目录内有 `build-info.txt`、`build-dependencies.txt` 和架构记录。

麒麟官方 V10 SP1 2203/2303 发布说明列出的 x86 组件基线为 glibc 2.31。具体补丁版、浏览器系统库和内网环境仍需在目标电脑确认。可在麒麟桌面终端运行：

```bash
./启动.sh --self-check
```

这个检查会验证 Tk 队列界面、手动 URL 入口、SQLite、常驻服务、Playwright 驱动和附带 Chromium 启动，不访问 TMIS。若提示缺少系统动态库，请保留错误中缺失库的完整名称；不要替换系统 glibc。启动器会在 CPU 架构、glibc 或图形桌面环境不符合时给出明确提示。

构建配置加入单元测试、本地模拟浏览器测试和 Tk 控件测试，覆盖三类表单、队列追加、暂停、失败重试和文件落盘。**本轮没有触发新版 CI；本地测试不访问真实 TMIS，也不能验证账号权限、真实报表内容、Token 有效期或目标电脑证书配置。**请在新包生成后，先在目标机验证各类报表地市/省级小样本，再检查追加、重试和恢复，最后执行大批量任务。

技术依据：[PyInstaller Linux 兼容性](https://pyinstaller.org/en/stable/usage.html#making-gnu-linux-apps-forward-compatible)、[PyInstaller 外部程序动态库环境](https://pyinstaller.org/en/stable/common-issues-and-pitfalls.html#launching-external-programs-from-the-frozen-application)、[Playwright 打包说明](https://playwright.dev/python/docs/library#pyinstaller)、[麒麟 V10 SP1 发布说明](https://kylinos.cn/upload/product/20230509/466b4da07a7220fc1442901e42b560a7.pdf)。
