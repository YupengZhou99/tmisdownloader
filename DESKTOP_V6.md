# TMIS Workbench V6.2 · 长时间运行保护版 · 麒麟 x64

浅色 Electron + React + TypeScript 界面，复用原 Python 报表引擎与 SQLite 队列。每个工作区有独立 Python 后台、浏览器、参数表、保存目录和任务库，最多同时运行四路，工作区之间不自动转移任务；每一路内部仍按行串行执行。收入、支出、库存的数据自由查询字段和导出规则沿用既有程序；不是“总库数据自由查询”。

## 下载与启动

在 GitHub Actions 的 **Build TMIS Workbench V6.2 Kylin x64** 成功运行中下载 `TMIS-Workbench-V6.2-Kylin-x64` artifact。解开 artifact ZIP，再解开其中的 `.tar.gz`，完整保留目录结构。在图形桌面的终端运行：

```bash
cd TMIS-Workbench-Kylin-x64
./启动.sh
```

目标：银河麒麟 V10 SP1、x86_64、glibc ≥ 2.31。内含 Python 后台、Electron 和抓取用 Chromium，不需要联网安装 Python / Node / 浏览器。不要将压缩包内某一个文件单独复制出来。

使用普通桌面用户，不使用 sudo。为悬浮窗的位置、置顶和吸附功能，应用使用 X11 / XWayland；纯 Wayland 且没有 XWayland 的桌面不适用此启动方式。OS 窗口管理器仍可能限制置顶行为。

若报 Chromium sandbox / user namespace 错误，请系统管理员核对麒麟安全策略，不要直接添加 `--no-sandbox`。CI 的 root 容器验收例外不会写进发布启动器。

## 推荐使用顺序

1. 在“工作区总览”点击“管理登录”，每行粘贴一个完整 URL，再点击“分配到工作区”。也可逐个创建并命名工作区。Linux 不捕获登录 URL。
2. 每一路可先选“有头”或“无头”，点击“单独登录”或“全部登录”。每个工作区独立检查是否进入工作界面；任何一路成功后，即可“进入已登录工作区”配置并运行，无需等待其他三路。重启后也可只重新登录需要恢复的一路；登录模式会记住，链接不会保存。
3. 分别“进入队列”，多选或拖入 Excel；为此工作区选择根目录并“检查参数”。确认所属工作区、报表类型、条数、日期和实际保存位置，再“加入队列”。
4. “开始本队列”仅控制当前一路；总览的“全部开始”并行启动已确认且登录就绪的队列，对未就绪队列明确提示跳过。导入本身不会启动空闲队列。
5. 运行中仍可向指定队列追加参数，或添加新的工作区。新工作区只需自身登录成功；不影响已在运行的队列。
6. “暂停本队列”和“全部暂停”都等待各自当前条结束。后台下载推荐真正的无头模式，不要以最小化有头浏览器代替。每个工作区可预约有头/无头切换；悬浮窗汇总四路状态，点击某一路回到对应队列。
7. 结束后各浏览器与服务继续开启。“待处理任务”只展示本队列失败记录；总览的“全部待处理任务”按所属工作区汇总。重试只在原工作区执行，成功任务不会重复执行。
8. “清空已完成”仅隐藏当前工作区的成功任务，保留未完成/失败/中断任务、报表文件、SQLite 历史及结果 CSV。不会删除报表，也不会因此允许静默重复导入。列表每页 60 条，批次侧栏显示最近 200 批；更早任务仍可通过搜索或分页查看。

每个根目录下先建立固定的工作区子目录，再为参数表生成独立批次子目录，保留 `下载结果.csv` 和 `重试记录.csv`。即使多个工作区选了同一根目录，文件也不混放。首次成功导入记住根目录，以后可为单个批次另选目录；偏好设置可以修改默认根目录。修改默认目录、工作区名称或源 Excel 都不改变已经加入任务的保存位置和参数快照。

同一内容在当前或其他已加载工作区重复导入时会提示，允许明确确认后建立独立批次，不自动合并或分流任务。未加载的历史归档库不参与全局重复扫描；需要比较时先恢复该工作区。预览有效 15 分钟，且只能提交到创建预览的工作区。切换工作区不会把先前的导入提交到另一个队列；未提交的界面草稿在离开该工作区后需重新选择。

默认不开启 Excel 后处理，保留原始导出；开启后沿用旧程序的清洗 / 汇总规则，可能改变数据内容。

## 窗口与浏览器的区别

| 操作 | 运行中行为 | 是否重启抓取浏览器 |
| --- | --- | --- |
| 主界面 ↔ 悬浮窗 | 立即切换，共享同一队列与进度 | 否 |
| 手动最小化有头浏览器 | 某些桌面会暂停绘制；检测到最小化时中断本条并暂停此队列，不批量判失败；还原或切无头后继续 | 否 |
| 有头 ↔ 无头 | 等当前条完成或失败，再切换；执行前可取消预约 | 是 |
| 暂停队列 | 当前条结束后暂停，后续任务不领取 | 否 |
| 队列结束 | 服务待命，允许继续追加和重试；系统资源紧张时可暂时挂起浏览器 | 通常否 |
| 明确退出应用 | 关闭浏览器、保存中断任务、停止服务 | 是 |

快捷键 `Ctrl + Shift + M`（macOS 为 `⌘ + Shift + M`）在工作台与整体悬浮窗间切换。悬浮窗可拖动、置顶开关、靠近屏幕边缘吸附；位置会记住，并在屏幕变化时约束到可见范围。所有浏览器模式操作只影响所在工作区，不改变其他路的模式。

真正的有头 / 无头不是窗口隐藏。切换时仅在内存中迁移 Cookie、localStorage、sessionStorage，然后重建浏览器、检查工作界面和登录状态。不会重放登录 URL 中的 token / ticket。当前固定的 Playwright 版本不迁移 IndexedDB、内存中的页面对象或服务器绑定浏览器的状态，因此 **不能保证所有 TMIS 登录会话都可迁移**。迁移失败会保留成功记录及剩余任务，等待新的登录链接，不连续消耗后续任务。

## 长时间运行与资源保护

- Linux 为本次应用创建独立、仅当前用户可访问的磁盘临时目录：默认 `~/.cache/tmis-dler/runtime/run-*`（遵从 XDG_CACHE_HOME），可用 `TMIS_RUNTIME_DIR` 指定磁盘目录。拒绝 tmpfs / ramfs 和不足 512 MiB 的目录。Electron 与下载浏览器避免把普通共享内存文件放进容量有限的 `/dev/shm`；这是缓解配额耗尽，不代表消除所有物理内存消耗或保证 `/dev/shm` 零使用。正常退出只清理本次临时目录，不清空系统 `/dev/shm`，不碰其他程序的文件。
- 每次保存报表后立即删除 Playwright 内部下载临时副本，保留用户选定目录中的最终文件。浏览器在任务边界按需重建：已经处理至少一条，且达到 10 条、存活 20 分钟或本工作区浏览器进程树 RSS 超过 1.5 GiB，任一条件成立即可。只回收本程序拥有的进程。RSS 可能重复计入共享页，用于保守阈值，不是整机实际物理内存的精确分摊。
- 每 5 秒记录资源。可用内存低于 3 / 2 / 1 GiB 时，并发上限分别降为 2 / 1 / 0；`/dev/shm` 达到 80% / 90% 分别降为 1 / 0，inode 耗尽或临时磁盘不足 512 MiB 时暂缓所有新任务。等待当前条结束后，将超额工作区的登录状态暂存在内存并关闭其浏览器；资源健康约 60 秒后逐步恢复。手动暂停不会被自动恢复覆盖。极端突发内存不足仍可能先于检测发生，不能保证永不崩溃。
- UI 只接收有界分页快照和增量日志，隐藏窗口不接收推送，悬浮窗不接收完整任务/日志数组。渲染器未确认消息时，只保留最新快照和最多 100 条增量日志；可见日志最多 300 条，避免界面卡住时积压 IPC。
- UI 异常会记录原因并限频重载，不停止独立下载后台。连续异常不无限重启；重新双击启动程序可尝试恢复已有异常主窗口。若整个主进程被系统终止，重新打开后按工作区登录恢复，不能恢复断电或强杀时未保存的内存登录态。
- 诊断文件在任务基础目录下 `diagnostics/stability.jsonl`，单文件约 2 MiB，最多 4 份；包括 Electron 各进程、工作区浏览器资源、系统可用内存、共享内存和异常原因，不记录报表正文或登录链接。发生异常先保存这几份文件与 `df -h /dev/shm`、`df -i /dev/shm`、`free -h` 的结果，再退出或重启。异常终止留下的本程序临时目录可能占用磁盘；程序不会冒险批量删除正在使用的目录。

“下拉字段不存在选项”现在仅用于已经正常显示的选项列表确实没有目标值。未展开、渲染延迟、多个菜单歧义、点击未生效会报告交互异常并暂停本工作区。无头模式不依赖物理窗口是否最小化，但仍受网络、TMIS 页面和系统资源影响。

## 本地保存与安全边界

- 抓取后台通过父子进程私有标准输入输出通信，不监听 HTTP 端口。
- UI 只加载包内资源，禁用 Node integration，启用 renderer sandbox、context isolation、CSP 和 IPC 来源检查。无 CDN 字体、遥测、云端上传或自动更新。
- 应用不主动把登录 URL、token、Cookie 或浏览器登录状态写入配置、任务库和日志；错误日志脱敏。运行时浏览器/操作系统仍可能使用受自身策略管理的临时文件和内存交换，不能将其宣称为取证意义上的“零落盘”。
- Linux 工作区注册表在 `~/.local/state/tmis-dler/workspaces.json`（遵从 XDG_STATE_HOME），新工作区的独立库在其 `workspaces/<ID>/queue.sqlite3`。macOS 基础目录是 `~/Library/Application Support/TMISDLer`。注册表只保存名称、默认目录、浏览器路径、归档状态等非凭据信息。任务参数、下载路径及报表本身按本机敏感数据管理。
- 旧队列保留在原目录，自动成为“默认工作区”，不搬动已有报表。首次以多工作区版打开已有库时，在持有队列锁后用 SQLite 备份 API 创建 `queue.pre-workspaces.sqlite3`（包含 WAL 内容），再恢复中断状态。备份不覆盖；成功任务不重跑。
- “断开连接”只关闭对应浏览器，保留队列。需先暂停并等待当前条和模式切换结束。“归档”关闭此工作区后台、移出活动列表，但不删除队列或文件，可在总览底部恢复。活动工作区上限为四个。
- 一个后台异常退出时，仅该工作区显示不可用，其他队列继续。提供新 URL 后原队列可恢复。应用重启后各会话需要各自重新登录，但不必全部登录才能运行；工作区与队列身份保持不变。
- 主窗口关闭会询问继续运行、悬浮窗或退出。没有静默托盘常驻；悬浮窗关闭回到工作台。
- 突然断电 / 强杀进程后，下次启动显示中断任务；需重新登录和继续，不自动复用过期链接。完整文件保存后立即记录路径，若随后临时清理被打断，重试复用该文件；文件落地与状态记录间极短的强杀窗口仍可能提示文件已存在，请核对，不会自动覆盖。

## 参数表口径提醒

本次只打包程序，不内置旧业务参数表。请使用你已确认的最新版收入、支出和库存参数表；旧库存 280 条表包含原“全辖”省级口径，不能代替后续确认的“本级”修订参数。不要重复导入旧表的省级行与修订省级表。程序不会擅自修改已有 Excel。

## 本机开发与验证

```bash
cd desktop
npm ci
npm run install:runtime
npm run build
npm start
```

开发入口默认使用上级目录 `.venv/bin/python`；可用 `TMIS_PYTHON` 指定具备 requirements-linux-x64.txt 对应依赖的解释器。旧 Tk 界面仍可通过原 Python 主文件启动，但不能与 V6 同时使用同一任务库。

`npm run dev` 提供只读外观预览（127.0.0.1:5173）；浏览器预览没有本机文件和任务服务。`TMIS_DEV_URL=http://127.0.0.1:5173 npm start` 可用于桌面开发。生产包不读取该 URL。

验证入口：

```bash
python -m unittest discover -s tests -p 'test_*.py' -v
TMIS_BROWSER_TESTS=1 python -m unittest discover -s tests -p 'test_report_browser.py' -v
npm --prefix desktop test
node desktop/scripts/smoke.cjs
node desktop/scripts/workspaces-smoke.cjs
node desktop/scripts/stability-soak.cjs
```

桌面验收脚本使用独立临时任务库、合成 localhost TMIS 页面和参数表，验证真实 Electron → IPC → Python → Chromium 下载。四工作区用例使用同一域名和相同 Cookie 名的独立会话，验证四路同时执行、导出文件的国库/日期及会话标识、单路崩溃、暂停、模式切换、追加和重试，以及应用重启后队列保留。单工作区原有回归仍保留。不接触真实 TMIS、用户任务库或业务数据。CI 在 glibc 2.31 环境执行打包后的应用；截图和验收 JSON 是独立 artifact。

发布流水线使用 256 MiB `/dev/shm` 容器，要求同一发布二进制通过四工作区至少 4 小时、至少 1000 次导出的长稳测试。模拟报表增加大画布和表格负载，记录每 5 秒的资源样本，并检查预热后的增长上限。只有全部通过才上传发布包；实际结果以该次 Actions 日志和包内 `stability-acceptance.json` 为准，不能仅据此说明文档声称测试通过。可用 `TMIS_SOAK_SECONDS` / `TMIS_SOAK_DOWNLOADS` 缩短本地调试，但短测不等于发布验收。

**验收边界：** 构建成功、Mac 本机交互或 Debian 兼容环境测试，均不能替代实际麒麟桌面窗口管理器、系统安全策略、TMIS 内网权限和真实报表内容验收。首次使用请先选一个市级和一个省级任务，人工核对导出后再运行完整队列。
